.new_listing_form_field_container
  - if @listing.listing_type.eql?("request")
    = form.label :title, t(".i_will") + "*:", :class => "input"
  -else
    = form.label :title, t(".i_need") + "*:", :class => "input"
  = form.text_field(:title, :class => "title_text_field", :maxlength => "100")